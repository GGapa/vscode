#include <iostream>
using namespace std;
int T, id;

int main() {
    cin >> T >> id;
    while(T--) {
        int n, q;
        cin >> n >> q;
        for(int i = 1; i <= n; i++) {
            int a;
            cin >> a;
        }
        for(int i = 1; i <= q; i++) {
            int a, b;
            cin >> a >> b;
            cout << 0 << "\n";
        }
        

    }
}