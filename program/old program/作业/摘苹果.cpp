/*
#include <iostream>
using namespace std;

int main()
{
	const int count = 10;
	int a[count], s, all=0,i=0;
	for (int i = 0; i < count; i++)
	{
		cin >> a[i];
	}
	cin >> s;
	for (int i = 0; i <count; i++)
	{
		if (a[i] <= s) all++;
		else if (a[i] >= s) {
			if (a[i] <= s + 30)all++;
		}
	}
	cout << all << endl;
	return 0;
}
*/