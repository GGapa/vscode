#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <algorithm>
typedef long long lld;
using namespace std;
int main() {
	freopen("0.in", "r", stdin);
	freopen("0.out", "w", stdout);
	fclose(stdin);
	fclose(stdout);
	return 0;
}
