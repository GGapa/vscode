/*
#include <iostream>
using namespace std;

int main()
{
	long long n, m, ans = 0, t;
	cin >> t;
	while (t--)
	{
		cin >> n >> m;
		if (m < n)swap(n, m);

		cout << n * 2 - 2 << endl;
	}
	return 0;
}
*/