#include <bits/stdc++.h>
#define rep(i, a, b) for(int i = (a), stOwxc = (b); i <= stOwxc; i++)
#define per(i, a, b) for(int i = (a), stOwxc = (b); i >= stOwxc; i--)
using namespace std;
using LL = long long;
using VI = vector<int>;

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int cnt = 0;
    while(1) {
        system("data.exe");
        system("ok.exe");
        system("B.exe");
        if(system("fc user.out data.out")) exit(0);
        cout << ++cnt << endl;
    }
    return 0;
}
