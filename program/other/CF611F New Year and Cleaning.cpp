#include <bits/stdc++.h>
#define rep(i, a, b) for(int i = (a), stOwxc = (b); i <= stOwxc; i++)
#define per(i, a, b) for(int i = (a), stOwxc = (b); i >= stOwxc; i--)
using namespace std;
using LL = long long;
using VI = vector<int>;
const int mod = 1e9 + 7;


unordered_map<char, int> fx = {{'U', -1}, {'D', -1}, {'L', 0}, {'R', 1}}, fy = {{'U', 0}, {'D', 0}, {'L', -1}, {'R', 1}};

signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int n, h, w; cin >> n >> h >> w;
    string s; cin >> s; s = " " + s;
    return 0;
}
