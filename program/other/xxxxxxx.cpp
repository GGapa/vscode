#include <bits/stdc++.h>
#define rep(i, a, b) for(int i = (a), stOwxc = (b); i <= stOwxc; i++)
#define per(i, a, b) for(int i = (a), stOwxc = (b); i >= stOwxc; i--)
using namespace std;
using LL = long long;
using VI = vector<int>;
mt19937_64 rs(time(0));
signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int m=1e9;
    int n=rs()%m+1;
    // while(!check(n)) ++n;
    
    return 0;
}